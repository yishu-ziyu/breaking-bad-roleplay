# Handoff — 2026-07-02

## Current state

The Docker VM deployment is live and playable.

- Public domain: `https://bb.yishuziyu.cn/`
- Public IP fallback: `http://121.89.90.68/`
- Server app directory: `/opt/breaking-bad-roleplay`
- Docker container: `bb-roleplay`
- App port: `8080`
- Nginx routes:
  - `/etc/nginx/conf.d/bb-roleplay.conf` routes `bb.yishuziyu.cn` to `127.0.0.1:8080`
  - `/etc/nginx/conf.d/red-herring-ip-api.conf` routes bare IP `121.89.90.68` to this project
  - `/etc/nginx/conf.d/red-herring.conf` still owns `gun.yishuziyu.cn`

Use HTTPS for the domain. HTTP domain traffic can be intercepted by Aliyun ICP checks even when the same app works over HTTPS.

## What changed

- Docker build now works on the China-hosted VM:
  - npm uses `https://registry.npmmirror.com`
  - pip uses `https://pypi.tuna.tsinghua.edu.cn/simple`
  - macOS AppleDouble files are ignored in `.dockerignore`
- Alembic no longer passes the database URL through `configparser`, which avoids `%40` password interpolation failures.
- Database URL rendering keeps special password characters percent-encoded for SQLAlchemy.
- Added migration `a1b2c3d4e5f6_add_session_current_mode.py` for `sessions.current_mode`.
- StepFun provider calls now fallback to `MiniMax-M3` on HTTP errors when MiniMax is configured.
- Landing page entry is fixed with `hasEnteredWorld`, so clicking `ENTER THE WORLD` enters the app and starts the default Story flow.
- Landing language follows browser preference on first visit while keeping runtime language non-null.

## Environment notes

- Supabase direct DB hostname resolved only to IPv6 from this VM, and the VM does not have working IPv6 DB connectivity.
- The deployed app uses Supabase pooler instead of direct DB.
- Keep the DB password URL-encoded in `DATABASE_URL`; do not decode `%40` back to raw `@`.
- Do not commit `.env` or deployment secrets.
- StepFun currently returns quota-related HTTP errors in production. MiniMax fallback is what keeps Story generation playable.

## Certificate and DNS

- DNS provider: Alibaba Cloud DNS / HiChina.
- `bb.yishuziyu.cn` A record points to `121.89.90.68`.
- Let’s Encrypt certificate path:
  - `/etc/letsencrypt/live/bb.yishuziyu.cn/fullchain.pem`
  - `/etc/letsencrypt/live/bb.yishuziyu.cn/privkey.pem`
- Certificate expiry: `2026-09-30 14:49:59 UTC`.
- The certificate was issued through manual DNS-01. It will need manual renewal before expiry unless an Aliyun DNS automation hook is added.

## Verification run

- `npm run build` — passed
- `cd backend && ./.venv/bin/pytest tests/test_provider_parsing.py tests/test_routes.py tests/test_db_url.py` — 27 passed, 1 existing StarletteDeprecationWarning
- `git diff --check` — passed
- Playwright public smoke on `https://bb.yishuziyu.cn/` — title `ABQ Roleplay Lab`, landing present, click enters Story UI
- Server check — `bb-roleplay` container up, Nginx routes `bb.yishuziyu.cn` to port 8080

## Open risks

- Docker VM public build does not currently inject `VITE_SUPABASE_URL` / `VITE_SUPABASE_PUBLISHABLE_KEY`, so guest play works, but browser-side Supabase auth/cloud sync may be unavailable on this deployment until Vite build env vars are supplied.
- TLS renewal is manual today.
- Nginx/cert/server setup is documented but not codified as IaC.
- StepFun quota must be restored if StepFun is meant to be the primary provider again.
- `.claude/commands/loop.md` is an unrelated untracked local file and was intentionally left out of this handoff commit.

## Suggested next agent entry point

Start with `DEVLOG.md`, this handoff file, then inspect:

- `Dockerfile`
- `backend/alembic/env.py`
- `backend/db/url.py`
- `backend/agents/provider.py`
- `src/App.tsx`
- `src/App.css`

For the next iteration, pick one lane:

- Deployment hardening: add Docker Compose or server provisioning docs, automate cert renewal, inject Vite Supabase env vars.
- Gameplay quality: improve Story beat UX and MiniMax/StepFun provider observability.
- Supabase Edge Functions migration: continue from `.ship/tasks/supabase-edge-migration/plan/plan.md` if that path becomes preferable again.
